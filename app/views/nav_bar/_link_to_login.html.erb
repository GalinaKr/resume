<nav class="navbar navbar-expand-lg navbar-dark bg-header mb-sm-5">
  <div class="container">
    <div class="d-flex align-items-center text-white">
      <% unless user_signed_in? %>
        <%= t('interface.registration.message.have_account') %> <%= link_to t('interface.registration.log_in'), new_user_session_path, class: 'btn btn-primary ml-2' %>
      <% end %>
      <br /><br />
    </div>
  </div>
</nav>
