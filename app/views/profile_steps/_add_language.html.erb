<!--Add Family Modal-->
<div class="modal fade" id="addLanguageModal" tabindex="-1" role="dialog" aria-labelledby="modalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title" id="modalLabel"><%= t('interface.steps.language.form.add_title') %></h1>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">×</span>
        </button>
      </div>
      <%= form_for :language, url: wizard_path, method: :put do |f| %>
        <%= render partial: 'forms/form_language', locals: { language: nil, f: f,
                                                           profile: @profile } %>
      <% end %>
    </div>
  </div>
</div>
